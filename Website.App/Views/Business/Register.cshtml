
@{
    ViewData["Title"] = "注册帐号";
    Layout = "~/Views/Shared/_LayoutTwo.cshtml";
}
@using (Html.BeginForm("register", "business", @FormMethod.Post, new { id = "form1", @class = "per_login" }))
{
    <div class="dk">
        <p>注册获取商机</p>
    </div>
    <div class="carMeg">
        <div class="xia">
            <div>
                <input id="txtMobile" class="tel" type="text" placeholder="请输入手机号码" />
            </div>
            <div style="margin-top:10px;">
                <input id="txtPwd" class="tel" type="text" placeholder="请输入密码" />
            </div>
            <div style="margin-top:10px;">
                <input id="txtRePwd" class="tel" type="text" placeholder="请确认密码" />
            </div>
            <div class="useryzm">
                <input id="txtCheckCode" name="txtCheckCode" class="carPhone_lastinput1 yzm" type="text" placeholder="请输入验证码" />
                <input id="btn-send-code" class="carPhone_lastinput2" type="button" value="发送验证码" />
            </div>
            <div class="tips" id="valiMsg"></div>
            <button id="submit" type="button" class="submit_button">注&nbsp;&nbsp;&nbsp;&nbsp;册</button>

        </div>
    </div>
}

@section Scripts{ 
    <script type="text/javascript">
        $("#btn-send-code").click(function () {
            var that = this;
            var strMobile = $.trim($("#txtMobile").val());
            if (strMobile != "") {
                $.post("/business/SendMsg", { mobile: strMobile }, function (data) {

                });
            } else { 

            }
            //$.ajax({

            //});
        });
    </script>
}
